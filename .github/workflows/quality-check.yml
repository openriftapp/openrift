name: Quality Check

on:
  push:
    branches: [main]
    paths-ignore:
      - "**/*.md"
      - "docs/**"
  pull_request:
    branches: [main]
    paths-ignore:
      - "**/*.md"
      - "docs/**"
  # Allow to run this workflow manually from the Actions tab
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

env:
  DO_NOT_TRACK: 1

jobs:
  build:
    # Do not run for automated release commits (otherwise we would run into a deadlock situation)
    if: ${{ !contains(github.event.head_commit.message, 'chore(release):') }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Cache Bun install store
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build
        run: bun run build

      # nginx serves the client build straight from the image, so an extension
      # its mime.types doesn't know ships as application/octet-stream.
      - name: Check asset content types
        run: bun run check:asset-types

      # Dependabot bumps bypass lefthook (bot commits don't run pre-commit
      # hooks), so a root `overrides` pin can drift from the package it dedupes
      # without anything catching it before merge.
      - name: Check override pins
        run: bun run check:override-pins

      # The web image ships .output alone, so a dependency the bundle neither
      # inlines nor traces into it builds fine and 500s every SSR request at
      # runtime. Booting the built server is the only thing that catches it.
      - name: Check SSR bundle boots
        run: bun run check:ssr-bundle

  test:
    if: ${{ !contains(github.event.head_commit.message, 'chore(release):') }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Cache Bun install store
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Cache Turborepo
        uses: actions/cache@v6
        with:
          path: .turbo
          key: turbo-test-${{ runner.os }}-${{ github.sha }}
          restore-keys: |
            turbo-test-${{ runner.os }}-

      - name: Test
        run: bun run test

  lint:
    if: ${{ !contains(github.event.head_commit.message, 'chore(release):') }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Cache Bun install store
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      # routeTree.gen.ts and src/paraglide are gitignored build artifacts.
      # Without them the type-aware rules see route context, search params and
      # message functions as `error`/`any`.
      - name: Generate route tree
        run: bun run routes:generate

      - name: Compile messages
        run: bun run --cwd apps/web paraglide:compile

      - name: Lint
        run: bun run lint
